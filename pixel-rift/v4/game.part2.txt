tupMode(){$('modeSubtitle').textContent=playerCount===2?'同畫面合作 × 各自生命 × 專屬流派 × 多種 Boss × 變異魔王':'單人專屬流派 × 輔助角可獨立成型 × 多敵人 × 多關主 × 變異魔王';$('modeBadge').textContent=playerCount===2?'2 PLAYERS / LOCAL CO-OP':'1 PLAYER / SOLO BUILD';$('p2Card').style.display=playerCount===2?'block':'none';$('p2Help').style.display=playerCount===2?'block':'none';$('selectors').style.gridTemplateColumns=playerCount===2?'repeat(2,1fr)':'1fr';$('controlsGrid').style.gridTemplateColumns=playerCount===2?'repeat(2,1fr)':'1fr';$('p2Status').style.display=playerCount===2?'block':'none';$('p1Status').classList.toggle('single',playerCount===1);$('startBtn').textContent=playerCount===2?'開始雙人裂隙':'開始單人裂隙';$('upgradeSubtitle').textContent=playerCount===2?'共享通用強化；角色專屬強化只會套用到對應角色。':'通用與角色專屬強化會混合出現，形成本局流派。'}
function makePlayer(hero,index,x,y){return{id:index,hero:hero.id,name:hero.name,x,y,r:10,hp:hero.hp,maxHp:hero.hp,shield:0,speed:hero.speed,damage:hero.damage,rate:hero.rate,color:hero.color,pierce:hero.pierce,crit:hero.crit,projectiles:1,magnet:90,armor:0,shotT:0,shotCount:0,dashCd:0,dashing:0,dx:0,dy:0,invuln:0,dead:false,revive:0,gpDash:false,gale:0,hunterMark:0,hunterT:0,thorns:0,fortress:0,chain:0,nova:0,novaTimer:0,pulseCd:5.3,pulseTimer:1.2,pulseHeal:.035,pulseDamage:1.15,pulseRadius:170,pulseShield:0,drones:0,droneT:0,droneA:0}}
function newGame(){const cx=WORLD_W/2,cy=WORLD_H/2,players=[makePlayer(HEROES[p1Choice],1,cx-(playerCount===2?28:0),cy)];if(playerCount===2)players.push(makePlayer(HEROES[p2Choice],2,cx+28,cy));game={t:0,wave:1,waveT:0,waveLen:27,spawnT:0,score:0,kills:0,coins:0,xp:0,xpNeed:22,level:1,players,enemies:[],bullets:[],enemyBullets:[],gems:[],particles:[],texts:[],camera:{x:cx,y:cy,zoom:.76},boss:null,ended:false};state='playing';$('menu').classList.remove('show');$('gameover').classList.remove('show');$('pause').classList.remove('show');$('hud').hidden=false;for(let i=0;i<18;i++)spawnEnemy(true);last=performance.now();toast(playerCount===2?'雙人裂隙已開啟！':'裂隙已開啟！')}
function alivePlayers(){return game.players.filter(p=>!p.dead)}function centerPlayers(){const a=alivePlayers();if(!a.length)return{x:WORLD_W/2,y:WORLD_H/2};return{x:a.reduce((s,p)=>s+p.x,0)/a.length,y:a.reduce((s,p)=>s+p.y,0)/a.length}}
function nearestPlayer(from){let t=null,b=Infinity;for(const p of alivePlayers()){const q=d2(from,p);if(q<b){b=q;t=p}}return t}
function spawnPoint(initial=false){const ctr=centerPlayers(),z=game.camera.zoom,viewR=Math.hypot(W/z,H/z)/2,a=Math.random()*TAU,r=initial?rand(190,440):rand(viewR*.8,viewR*1.08);return{x:clamp(ctr.x+Math.cos(a)*r,35,WORLD_W-35),y:clamp(ctr.y+Math.sin(a)*r,35,WORLD_H-35)}}
function availableEnemyTypes(){const w=game.wave,a=['chaser'];if(w>=2)a.push('shooter');if(w>=3)a.push('tank','splitter');if(w>=4)a.push('assassin');if(w>=5)a.push('bomber');if(w>=6)a.push('healer');if(w>=7)a.push('sniper');if(w>=8)a.push('summoner');return a}
function makeEnemy(type,pos,scale=1){const d=ENEMY_DEFS[type];return{x:pos.x,y:pos.y,type,name:d.name,r:d.r,hp:d.hp*scale,maxHp:d.hp*scale,speed:d.speed*(1+game.wave*.018),damage:d.damage*(1+game.wave*.06),color:d.color,xp:d.xp,score:d.score,shotT:rand(.6,1.8),skillT:rand(1.5,3.5),chargeT:rand(1.2,2.5),dashT:0,healT:2.5,summonT:3.2,telegraph:0,dead:false,hit:0}}
function spawnEnemy(initial=false,type=null){if(!game)return;const list=availableEnemyTypes();type=type||list[Math.floor(Math.random()*list.length)];const scale=(1+(game.wave-1)*.12)*(type==='mini'?.75:1);game.enemies.push(makeEnemy(type,spawnPoint(initial),scale))}
function spawnBoss(){const idx=Math.floor(game.wave/5-1)%BOSS_TYPES.length,b=BOSS_TYPES[idx],mutant=game.wave%15===0,scale=Math.pow(1.32,Math.floor(game.wave/5)-1)*(playerCount===2?1.55:1);let hp=b.hp*scale;if(mutant)hp*=3.1;const pos=spawnPoint(false),boss={x:pos.x,y:pos.y,type:'boss',bossKind:b.id,bossName:(mutant?'變異·':'')+b.name,r:mutant?38:31,hp,maxHp:hp,speed:b.speed*(mutant?1.1:1),damage:b.damage*(1+game.wave*.055)*(mutant?1.45:1),color:mutant?'#ff4ad8':b.color,xp:mutant?240:100,score:mutant?12000:3200,shotT:.8,skillT:2.2,burstT:2.7,summonT:3.3,phase2:false,mutant,mutations:[],armorRebuilt:false,dead:false,hit:0};if(mutant){const pool=[...MUTATIONS];for(let i=0;i<2;i++)boss.mutations.push(pool.splice(Math.floor(Math.random()*pool.length),1)[0])}game.enemies.push(boss);game.boss=boss;toast(mutant?'☠ 變異魔王降臨：'+boss.bossName:'⚠ 關主降臨：'+boss.bossName);shake=12}
function nearestEnemy(from,exclude=null,max=Infinity){let t=null,b=max*max;for(const e of game.enemies){if(e.dead||e===exclude)continue;const q=d2(from,e);if(q<b){b=q;t=e}}return t}
function fire(p){const target=nearestEnemy(p);if(!target)return;let count=p.projectiles;p.shotCount++;if(p.hero==='ranger'&&p.gale&&p.shotCount%10===0)count+=2*p.gale;const base=Math.atan2(target.y-p.y,target.x-p.x),spread=.13;for(let i=0;i<count;i++){const a=base+(i-(count-1)/2)*spread,special=p.hero==='ranger'&&p.shotCount%6===0;game.bullets.push({owner:p,x:p.x,y:p.y,vx:Math.cos(a)*435,vy:Math.sin(a)*435,r:special?5:3,damage:p.damage*(special?1.55:1),pierce:p.pierce+(special?2:0),life:1.75,hit:new Set(),color:special?'#fff6a6':p.color})}}
function fireDrone(p){if(!p.drones)return;p.droneA+=.7;for(let i=0;i<p.drones;i++){const a=p.droneA+i/p.drones*TAU,ox=p.x+Math.cos(a)*54,oy=p.y+Ma