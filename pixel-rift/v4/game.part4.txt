s(a)*290;e.dashVY=Math.sin(a)*290;e.dashT=.45}if(e.mutant&&e.mutations.some(m=>m.id==='volatile'))for(let i=0;i<12;i++)addEnemyBullet(e.x,e.y,i/12*TAU,190,e.damage*.34,4,'#ff76df')}
function applyMutations(e,dt){if(!e.mutant)return;if(e.hp/e.maxHp<.5&&!e.phase2){e.phase2=true;toast('☠ 變異魔王進入第二型態！');shake=15}if(e.mutations.some(m=>m.id==='fortified')&&e.hp/e.maxHp<.5&&!e.armorRebuilt){e.armorRebuilt=true;e.hp=Math.min(e.maxHp,e.hp+e.maxHp*.18);toast('◆ 晶甲重構：魔王恢復生命')}if(e.mutations.some(m=>m.id==='swarm')){e.summonT-=dt;if(e.summonT<=0){e.summonT=e.phase2?3.2:4.8;for(let i=0;i<3;i++)game.enemies.push(makeEnemy(i===0?'assassin':'chaser',{x:e.x+rand(-90,90),y:e.y+rand(-90,90)},1+game.wave*.05))}}}
function gainXp(v){game.xp+=v;if(game.xp>=game.xpNeed){game.xp-=game.xpNeed;game.level++;game.xpNeed=Math.floor(game.xpNeed*1.28+9);showUpgrade()}}
function eligibleUpgrades(){const ids=new Set(game.players.map(p=>p.hero));return UPGRADES.filter(u=>!u.heroes||u.heroes.some(h=>ids.has(h)))}
function showUpgrade(){state='upgrade';const pool=[...eligibleUpgrades()],choices=[];while(choices.length<3&&pool.length)choices.push(pool.splice(Math.floor(Math.random()*pool.length),1)[0]);$('upgradeCards').innerHTML='';for(const u of choices){const el=document.createElement('article');el.className='upgrade';const support=u.heroes?.includes('support');el.innerHTML=`<h3>${u.name}</h3><p>${u.desc}</p><span class="tag ${support?'support-tag':''}">${u.tag}</span>`;el.onclick=()=>{for(const p of game.players){if(!u.heroes||u.heroes.includes(p.hero))u.apply(p)}$('upgrade').classList.remove('show');state='playing';last=performance.now();toast(u.name+' 已獲得！')};$('upgradeCards').appendChild(el)}$('upgrade').classList.add('show')}
function dash(p){if(!p||p.dead||p.dashCd>0||state!=='playing')return;let dx=p.dx,dy=p.dy;if(Math.hypot(dx,dy)<.1){dx=p.id===1?1:-1;dy=0}const l=Math.hypot(dx,dy)||1;p.dashVX=dx/l*520;p.dashVY=dy/l*520;p.dashing=.18;p.dashCd=3.2;burst(p.x,p.y,p.color,12)}
function inputs(p){let mx=0,my=0;if(p.id===1){mx=(keys.d?1:0)-(keys.a?1:0);my=(keys.s?1:0)-(keys.w?1:0)}else{mx=(keys.ArrowRight?1:0)-(keys.ArrowLeft?1:0);my=(keys.ArrowDown?1:0)-(keys.ArrowUp?1:0)}const pads=navigator.getGamepads?navigator.getGamepads():[],gp=pads[p.id-1];if(gp){const gx=Math.abs(gp.axes[0]||0)>.18?gp.axes[0]:0,gy=Math.abs(gp.axes[1]||0)>.18?gp.axes[1]:0;if(Math.hypot(gx,gy)>.1){mx=gx;my=gy}if(gp.buttons[0]?.pressed&&!p.gpDash)dash(p);p.gpDash=gp.buttons[0]?.pressed}const l=Math.hypot(mx,my);if(l>1){mx/=l;my/=l}return[mx,my]}
function updateEnemy(e,dt){if(e.dead)return;if(e.type==='boss'){applyMutations(e,dt);e.skillT-=dt;e.shotT-=dt;if(e.skillT<=0)bossSkill(e);if(e.shotT<=0)shootEnemy(e)}const p=nearestPlayer(e);if(!p)return;const a=Math.atan2(p.y-e.y,p.x-e.x),d=Math.hypot(p.x-e.x,p.y-e.y);let ideal=0;if(['shooter','healer','summoner'].includes(e.type))ideal=200;if(e.type==='sniper')ideal=330;if(e.type==='boss')ideal=e.bossKind==='behemoth'?80:115;let dir=d>ideal+18?1:ideal&&d<ideal-35?-1:0;if(e.dashT>0){e.dashT-=dt;e.x+=e.dashVX*dt;e.y+=e.dashVY*dt}else{e.x+=Math.cos(a)*e.speed*dir*dt;e.y+=Math.sin(a)*e.speed*dir*dt}e.x=clamp(e.x,20,WORLD_W-20);e.y=clamp(e.y,20,WORLD_H-20);
 if(e.type==='shooter'){e.shotT-=dt;if(e.shotT<=0)shootEnemy(e)}
 if(e.type==='assassin'){e.chargeT-=dt;if(e.chargeT<=0){e.chargeT=2.6;e.dashVX=Math.cos(a)*410;e.dashVY=Math.sin(a)*410;e.dashT=.28}}
 if(e.type==='bomber'&&d<72){damagePlayersInRadius(e.x,e.y,82,e.damage,e);game.particles.push({type:'ring',x:e.x,y:e.y,r:8,maxR:90,life:.3,max:.3,color:'#ff7556'});e.dead=true;game.kills++;return}
 if(e.type==='healer'){e.healT-=dt;if(e.healT<=0){e.healT=3;for(const q of game.enemies)if(q!==e&&!q.dead&&d2(q,e)<170*170)q.hp=Math.min(q.maxHp,q.hp+q.maxHp*.08);game.particles.push({type:'ring',x:e.x,y:e.y,r:8,maxR:170,life:.3,max:.3,color:'#77ffc0'})}}
 if(e.type==='summoner'){e.summonT-=dt;if(e.summonT<=0){e.summonT=4.1;for(let i=0;i<2;i++)game.enemies.push(makeEnemy('mini',{x:e.x+rand(-35,35),y:e.y+rand(-35,35)},1+game.wave*.03))}}
 if(e.type==='sniper'){e.shotT-=dt;if(e.shotT<=0)shootEnemy(e)}
 for(const q of alivePlayers())if(d2(q,e)<(q.r+e.r)**2)hurt(q,e.damage,e.x,e.y,e)}
function update(dt){if(state!=='playing'||!game)return;dt=Math.min(dt,.033);game.t+=dt;game.waveT+=dt;if(game.waveT>=game.waveLen){game.wave++;game.waveT=0;game.score+=game.wave*260;toast('WAVE '+game.wave);if(game.wave%5===0)spawnBoss()}game.spawnT-=dt;const cap=Math.min(115,10+game.wave*4+(playerCount===2?8:0));if(game.spawnT<=0&&game.enemies.length<cap){spawnEnemy();game.spawnT=Math.max(.1,.7-game.wave*.03)}
 for(const p of game.players){if(p.dead)continue;const [mx,my]=inputs(p);p.dx=mx;p.dy=my;p.invuln=Math.max(0,p.invuln-dt);p.dashCd=Math.max(0,p.dashCd-dt);if(p.hunterT>0)p.hunterT-=dt;if(p.dashing>0){p.dashing-=dt;p.x+=p.dashVX*dt;p.y+=p.dashVY*dt}else{p.x+=mx*p.speed*dt;p.y+=my*p.speed*dt}p.x=clamp(p.x,24,WORLD_W-24);p.y=clamp(p.y,24,WORLD_H-24);p.shotT-=dt;const rate=p.rate*(p.hunterT>0?1+p.hunterMark:1);if(p.shotT<=0&&game.enemies.length){fire(p);p.shotT=1/rate}if(p.hero==='support'){p.pulseTimer-=dt;if(p.pulseTimer<=0){p.pulseTimer=p.pulseCd;pulseSupport(p)}if(p.drones){p.droneT-=dt;if(p.droneT<=0){p.droneT=Math.max(.32,.85-p.drones*.09);fireDrone(p)}}}if(p.nova)