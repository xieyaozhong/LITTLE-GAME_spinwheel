}
function draw(){const sx=shake?rand(-shake,shake):0,sy=shake?rand(-shake,shake):0;ctx.save();ctx.translate(W/2+sx,H/2+sy);const z=game?game.camera.zoom:.76;ctx.scale(z,z);const cx=game?game.camera.x:WORLD_W/2,cy=game?game.camera.y:WORLD_H/2;ctx.translate(-cx,-cy);drawBackground(cx,cy,z);if(game){for(const g of game.gems){ctx.save();ctx.translate(g.x,g.y);ctx.rotate(Math.PI/4);rect(-g.r,-g.r,g.r*2,g.r*2,g.color);ctx.restore()}for(const q of game.particles)drawParticle(q);for(const b of game.bullets){rect(b.x-b.r,b.y-b.r,b.r*2,b.r*2,b.color)}for(const b of game.enemyBullets){ctx.fillStyle=b.color;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,TAU);ctx.fill()}for(const e of game.enemies)drawEnemy(e);for(const p of game.players)drawPlayer(p)}ctx.restore();if(flash){ctx.fillStyle=`rgba(255,70,110,${flash*2})`;ctx.fillRect(0,0,W,H)}}
function burst(x,y,color,n=8){if(!game)return;for(let i=0;i<n;i++){const a=Math.random()*TAU,s=rand(30,130),life=rand(.2,.55);game.particles.push({type:'dot',x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,size:rand(2,5),color,life,max:life})}}
function toast(msg){const t=$('toast');t.textContent=msg;t.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.remove('show'),1500)}
function togglePause(){if(state==='playing'){state='paused';$('pause').classList.add('show')}else if(state==='paused'){state='playing';$('pause').classList.remove('show');last=performance.now()}}
addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;keys[e.key]=true;if([' ','ArrowUp','ArrowDown','ArrowLeft','ArrowRight'].includes(e.key))e.preventDefault();if(e.key===' ')dash(game?.players[0]);if(playerCount===2&&e.key==='Enter')dash(game?.players[1]);if(e.key.toLowerCase()==='p'||e.key==='Escape')togglePause()},{passive:false});addEventListener('keyup',e=>{keys[e.key.toLowerCase()]=false;keys[e.key]=false});document.addEventListener('visibilitychange',()=>{if(document.hidden&&state==='playing')togglePause()});
$('p1Prev').onclick=()=>{p1Choice=(p1Choice+HEROES.length-1)%HEROES.length;updateHeroUI()};$('p1Next').onclick=()=>{p1Choice=(p1Choice+1)%HEROES.length;updateHeroUI()};$('p2Prev').onclick=()=>{p2Choice=(p2Choice+HEROES.length-1)%HEROES.length;updateHeroUI()};$('p2Next').onclick=()=>{p2Choice=(p2Choice+1)%HEROES.length;updateHeroUI()};$('startBtn').onclick=newGame;$('retryBtn').onclick=newGame;$('restartBtn').onclick=newGame;$('resumeBtn').onclick=togglePause;
function loop(now){const dt=(now-last)/1000;last=now;if(state==='playing')update(dt);draw();requestAnimationFrame(loop)}setupMode();updateHeroUI();updateMenuStats();draw();requestAnimationFrame(loop);
})();