{p.novaTimer-=dt;if(p.novaTimer<=0){p.novaTimer=p.nova;areaDamage(p.x,p.y,265,p.damage*1.2,p,'#c8b1ff')}}}
 if(playerCount===2)for(const down of game.players.filter(p=>p.dead)){const saver=game.players.find(p=>!p.dead&&d2(p,down)<75*75);if(saver){down.revive+=dt;if(down.revive>=3){down.dead=false;down.hp=down.maxHp*.42;down.invuln=1.5;down.revive=0;burst(down.x,down.y,'#fff5ae',28);toast('隊友已救援！')}}else down.revive=Math.max(0,down.revive-dt*.65)}
 for(const e of [...game.enemies])updateEnemy(e,dt);
 for(const b of game.bullets){b.x+=b.vx*dt;b.y+=b.vy*dt;b.life-=dt;for(const e of game.enemies){if(e.dead||b.hit.has(e))continue;if(d2(b,e)<(b.r+e.r)**2){b.hit.add(e);damageEnemy(e,b.damage,b.owner,true);if(b.pierce>0)b.pierce--;else b.life=0;if(b.life<=0)break}}}
 for(const b of game.enemyBullets){b.x+=b.vx*dt;b.y+=b.vy*dt;b.life-=dt;for(const p of alivePlayers())if(d2(b,p)<(b.r+p.r)**2){b.life=0;hurt(p,b.damage,b.x,b.y,null);break}}
 for(const gem of game.gems){let p=null,b=Infinity;for(const q of alivePlayers()){const z=d2(gem,q);if(z<b){b=z;p=q}}if(!p)continue;const d=Math.sqrt(b);if(d<p.magnet){const sp=260+(p.magnet-d)*4;gem.x+=(p.x-gem.x)/Math.max(1,d)*sp*dt;gem.y+=(p.y-gem.y)/Math.max(1,d)*sp*dt}if(d<p.r+gem.r+5){gem.dead=true;gainXp(gem.value)}}
 for(const q of game.particles){q.life-=dt;if(q.type==='dot'){q.x+=q.vx*dt;q.y+=q.vy*dt;q.vx*=.95;q.vy*=.95}if(q.type==='ring')q.r+=(q.maxR-q.r)*dt*9}for(const t of game.texts){t.y-=25*dt;t.life-=dt}
 game.enemies=game.enemies.filter(e=>!e.dead);game.bullets=game.bullets.filter(b=>b.life>0&&b.x>-80&&b.x<WORLD_W+80&&b.y>-80&&b.y<WORLD_H+80);game.enemyBullets=game.enemyBullets.filter(b=>b.life>0);game.gems=game.gems.filter(g=>!g.dead);game.particles=game.particles.filter(q=>q.life>0);game.texts=game.texts.filter(t=>t.life>0);game.score+=dt*(14+game.wave*2.2);const al=alivePlayers(),ctr=centerPlayers();let targetZoom=.77;if(al.length>1){const sep=Math.hypot(al[0].x-al[1].x,al[0].y-al[1].y);targetZoom=clamp(.82-sep/2400,.54,.78)}const f=1-Math.exp(-dt*5.5);game.camera.x+=(ctr.x-game.camera.x)*f;game.camera.y+=(ctr.y-game.camera.y)*f;game.camera.zoom+=(targetZoom-game.camera.zoom)*f;shake=Math.max(0,shake-dt*25);flash=Math.max(0,flash-dt);updateHud()}
function updateHud(){const a=game.players[0],b=game.players[1];$('p1HeroLabel').textContent=a.name;$('p1HpText').innerHTML=a.dead?`<span class="downed">DOWN ${Math.round(a.revive/3*100)}%</span>`:`${Math.ceil(a.hp)} / ${Math.ceil(a.maxHp)}${a.shield>0?' +◇'+Math.ceil(a.shield):''}`;$('p1HpBar').style.width=(a.dead?0:clamp(a.hp/a.maxHp,0,1)*100)+'%';if(b){$('p2HeroLabel').textContent=b.name;$('p2HpText').innerHTML=b.dead?`<span class="downed">DOWN ${Math.round(b.revive/3*100)}%</span>`:`${Math.ceil(b.hp)} / ${Math.ceil(b.maxHp)}${b.shield>0?' +◇'+Math.ceil(b.shield):''}`;$('p2HpBar').style.width=(b.dead?0:clamp(b.hp/b.maxHp,0,1)*100)+'%'}$('waveText').textContent=game.wave;$('levelText').textContent=game.level;$('coinText').textContent=game.coins;$('scoreText').textContent=Math.floor(game.score).toLocaleString();const m=(game.t/60)|0,s=(game.t%60)|0;$('timeText').textContent=String(m).padStart(2,'0')+':'+String(s).padStart(2,'0');$('reviveNote').style.display=playerCount===2&&game.players.some(p=>p.dead)&&alivePlayers().length?'block':'none';if(game.boss&&!game.boss.dead){$('bossBox').style.display='block';$('bossName').textContent=game.boss.bossName;$('bossBar').style.width=clamp(game.boss.hp/game.boss.maxHp,0,1)*100+'%';$('bossAffixes').textContent=game.boss.mutant?'變異詞綴：'+game.boss.mutations.map(m=>m.name+'（'+m.desc+'）').join(' × '):'關主技能：'+BOSS_TYPES.find(b=>b.id===game.boss.bossKind).skill}else $('bossBox').style.display='none'}
function endGame(){if(!game||game.ended)return;game.ended=true;state='gameover';const earned=game.coins+Math.floor(game.wave*1.6)+Math.floor(game.score/2200);save.coins=(save.coins||0)+earned;save.totalKills=(save.totalKills||0)+game.kills;save.bestScore=Math.max(save.bestScore||0,Math.floor(game.score));save.bestWave=Math.max(save.bestWave||0,game.wave);persist();updateMenuStats();$('hud').hidden=true;$('endWave').textContent=game.wave;$('endScore').textContent=Math.floor(game.score).toLocaleString();$('endKills').textContent=game.kills;$('endCoins').textContent=earned;$('gameOverText').textContent=playerCount===2?'兩名玩家都被裂隙吞噬了':'你被裂隙吞噬了';$('gameover').classList.add('show')}
function rect(a,b,w,h,col){ctx.fillStyle=col;ctx.fillRect(Math.round(a),Math.round(b),Math.round(w),Math.round(h))}
function drawBackground(cx,cy,z){const l=cx-W/(2*z)-60,r=cx+W/(2*z)+60,t=cy-H/(2*z)-60,b=cy+H/(2*z)+60;ctx.fillStyle='#0b0822';ctx.fillRect(l,t,r-l,b-t);const g=48,sx=Math.floor(l/g)*g,sy=Math.floor(t/g)*g;ctx.strokeStyle='rgba(100,78,178,.15)';ctx.lineWidth=1;for(let q=sx;q<r;q+=g){ctx.beginPath();ctx.moveTo(q,t);ctx.lineTo(q,b);ctx.stroke()}for(let q=sy;q<b;q+=g){ctx.beginPath();ctx.moveTo(l,q);ctx.lineTo(r,q);ctx.stroke()}ctx.strokeStyle='rgba(255,93,162,.28)';ctx.lineWidth=4;ctx.strokeRect(0,0,WORLD_W,WORLD_H)}
function drawPlayer(p){ctx.save();ctx.translate(Math.round(p.x),Math.round(p.y));if(p.dead)ctx.globalAlpha=.32;else if(p.invuln>0&&((p.invuln*15)|0)%2===0)ctx.globalAlpha=.35;if(p.dashing>0){rect(-22,-6,14,12,p.color);rect(-34,-3,9,6,'rgba(255,255,255,.5)')}rec