th.sin(a)*54,t=nearestEnemy({x:ox,y:oy},null,700);if(t){const q=Math.atan2(t.y-oy,t.x-ox);game.bullets.push({owner:p,x:ox,y:oy,vx:Math.cos(q)*390,vy:Math.sin(q)*390,r:2.5,damage:p.damage*.52,pierce:0,life:1.5,hit:new Set(),color:'#9fffd0'})}}}
function addEnemyBullet(x,y,a,speed,damage,r=4,color='#ffb15c'){game.enemyBullets.push({x,y,vx:Math.cos(a)*speed,vy:Math.sin(a)*speed,r,damage,life:5,color})}
function shootEnemy(e){const p=nearestPlayer(e);if(!p)return;const a=Math.atan2(p.y-e.y,p.x-e.x),mult=e.mutant&&e.mutations.some(m=>m.id==='temporal')?.72:1;if(e.type==='boss'){if(e.bossKind==='storm'){for(let i=0;i<7;i++)addEnemyBullet(e.x,e.y,a+(i-3)*.14,210,e.damage*.48,4,'#78cfff')}else{for(let i=-2;i<=2;i++)addEnemyBullet(e.x,e.y,a+i*.18,180,e.damage*.55,5,e.color)}e.shotT=(e.phase2?.72:1.05)*mult}else if(e.type==='sniper'){addEnemyBullet(e.x,e.y,a,310,e.damage,4,'#ffe39a');e.shotT=3}else{addEnemyBullet(e.x,e.y,a,150,e.damage,4,e.color);e.shotT=2.1}}
function damagePlayersInRadius(x,y,r,damage,source){for(const p of alivePlayers())if(d2(p,{x,y})<(p.r+r)**2)hurt(p,damage,x,y,source)}
function hurt(p,amount,ex,ey,source=null){if(p.dead||p.invuln>0||p.dashing>0)return 0;let reduction=p.armor;if(p.hero==='knight'&&p.hp/p.maxHp<.45)reduction=Math.min(.75,reduction+p.fortress);let dealt=amount*(1-reduction);if(p.shield>0){const s=Math.min(p.shield,dealt);p.shield-=s;dealt-=s}if(dealt>0)p.hp-=dealt;p.invuln=.7;shake=7;flash=.08;const a=Math.atan2(p.y-ey,p.x-ex);p.x+=Math.cos(a)*10;p.y+=Math.sin(a)*10;if(source&&p.thorns)damageEnemy(source,amount*p.thorns,p,false);if(p.hero==='knight')for(const e of game.enemies)if(!e.dead&&d2(p,e)<75*75)damageEnemy(e,p.damage*.7,p,false);if(source?.mutant&&source.mutations.some(m=>m.id==='vamp')&&dealt>0)source.hp=Math.min(source.maxHp,source.hp+dealt*.55);if(p.hp<=0){p.hp=0;p.dead=true;p.revive=0;if(!alivePlayers().length)endGame()}return dealt}
function chainFrom(e,owner,damage){if(!owner.chain)return;if(Math.random()<owner.chain){const n=nearestEnemy(e,e,150);if(n){game.particles.push({type:'line',x:e.x,y:e.y,x2:n.x,y2:n.y,life:.13,max:.13,color:'#a6fbff'});damageEnemy(n,damage*.55,owner,false)}}}
function damageEnemy(e,amount,owner,canChain=true){if(e.dead)return;const crit=Math.random()<(owner?.crit||0),d=amount*(crit?2:1);e.hp-=d;e.hit=.08;burst(e.x,e.y,crit?'#ffe36e':e.color,crit?7:3);if(owner?.hero==='ranger'&&crit&&owner.hunterMark){owner.hunterT=1.4}if(canChain&&owner)chainFrom(e,owner,d);if(e.hp<=0)killEnemy(e,owner)}
function killEnemy(e,owner){if(e.dead)return;e.dead=true;game.kills++;game.score+=e.score*(1+game.wave*.04);const n=e.type==='boss'?(e.mutant?35:20):e.type==='tank'?3:1;for(let i=0;i<n;i++)game.gems.push({x:e.x+rand(-12,12),y:e.y+rand(-12,12),r:e.type==='boss'?5:4,value:e.xp/n,color:e.mutant?'#ff74e8':e.type==='boss'?'#ffd166':'#6ee7ff'});if(Math.random()<(e.type==='boss'?1:.08)){game.coins+=e.mutant?18:e.type==='boss'?7:1}if(e.type==='splitter')for(let i=0;i<2;i++){const q=makeEnemy('mini',{x:e.x+rand(-8,8),y:e.y+rand(-8,8)},1+(game.wave-1)*.08);game.enemies.push(q)}if(e.type==='boss'){game.score+=e.mutant?13000:5200;game.coins+=e.mutant?20:8;game.boss=null;toast(e.mutant?'★ 變異魔王已擊破！':'★ 關主已擊破！')}burst(e.x,e.y,e.color,e.type==='boss'?45:14)}
function pulseSupport(p){const heal=p.maxHp*p.pulseHeal;p.hp=Math.min(p.maxHp,p.hp+heal);if(p.pulseShield)p.shield=Math.min(p.maxHp*.45,p.shield+p.pulseShield);for(const ally of alivePlayers())if(ally!==p&&d2(p,ally)<250*250){ally.hp=Math.min(ally.maxHp,ally.hp+ally.maxHp*p.pulseHeal*.8);if(p.pulseShield)ally.shield=Math.min(ally.maxHp*.35,ally.shield+p.pulseShield)}for(const e of game.enemies)if(!e.dead&&d2(p,e)<(p.pulseRadius+e.r)**2)damageEnemy(e,p.damage*p.pulseDamage,p,false);game.particles.push({type:'ring',x:p.x,y:p.y,r:8,maxR:p.pulseRadius,life:.35,max:.35,color:'#7dffb2'});toast(playerCount===2?'♫ 共鳴脈衝：治療＋攻擊':'♫ 獨奏共鳴')}
function areaDamage(x,y,r,damage,owner,color='#ffb96a'){game.particles.push({type:'ring',x,y,r:8,maxR:r,life:.3,max:.3,color});for(const e of game.enemies)if(!e.dead&&d2(e,{x,y})<(r+e.r)**2)damageEnemy(e,damage,owner,false)}
function bossSkill(e){const p=nearestPlayer(e);if(!p)return;const temporal=e.mutant&&e.mutations.some(m=>m.id==='temporal');let cd=temporal?1.7:2.7;if(e.phase2)cd*=.72;e.skillT=cd;const a=Math.atan2(p.y-e.y,p.x-e.x);if(e.bossKind==='warden'){for(let i=0;i<20;i++)addEnemyBullet(e.x,e.y,i/20*TAU,135,e.damage*.42,4,e.color);e.dashVX=Math.cos(a)*360;e.dashVY=Math.sin(a)*360;e.dashT=.35}else if(e.bossKind==='storm'){for(let i=0;i<16;i++)addEnemyBullet(e.x,e.y,i/16*TAU+game.t*.7,155,e.damage*.38,4,'#7bdcff')}else if(e.bossKind==='brood'){for(let i=0;i<4;i++){const q=makeEnemy(i%2?'splitter':'chaser',{x:e.x+rand(-70,70),y:e.y+rand(-70,70)},1+game.wave*.06);game.enemies.push(q)}}else if(e.bossKind==='bishop'){e.hp=Math.min(e.maxHp,e.hp+e.maxHp*.045);for(let i=0;i<2;i++){const q=makeEnemy('healer',{x:e.x+rand(-80,80),y:e.y+rand(-80,80)},1+game.wave*.04);game.enemies.push(q)}for(let i=0;i<10;i++)addEnemyBullet(e.x,e.y,i/10*TAU,125,e.damage*.34,4,'#dca4ff')}else if(e.bossKind==='behemoth'){damagePlayersInRadius(e.x,e.y,145,e.damage*.75,e);game.particles.push({type:'ring',x:e.x,y:e.y,r:10,maxR:155,life:.35,max:.35,color:'#ffd166'});e.dashVX=Math.co