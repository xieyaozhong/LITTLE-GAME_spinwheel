// PIXEL RIFT V6 addon injected inside the V5 core IIFE
const V6_RARITIES=[
{id:'common',name:'普通',color:'#d6d0e8',mult:1},
{id:'uncommon',name:'精良',color:'#78f5a5',mult:1.15},
{id:'rare',name:'稀有',color:'#69b7ff',mult:1.32},
{id:'epic',name:'史詩',color:'#c98cff',mult:1.55},
{id:'legendary',name:'傳說',color:'#ffd166',mult:1.85}
];
const V6_SLOTS=[['weapon','武器'],['core','核心'],['charm','遺物']];
const V6_ELITES=[
{id:'swift',name:'迅捷',apply:e=>{e.speed*=1.35;e.baseSpeed=e.speed}},
{id:'brutal',name:'兇殘',apply:e=>e.damage*=1.4},
{id:'bulwark',name:'鐵壁',apply:e=>{e.maxHp*=1.75;e.hp=e.maxHp}},
{id:'regen',name:'再生',apply:e=>{}},
{id:'volatile',name:'爆裂',apply:e=>{}},
{id:'vamp',name:'嗜血',apply:e=>{}}
];
const V6_MAP_BOSSES={
wastes:{name:'荒原獵皇・裂牙王',kind:'warden',color:'#ff547d'},
forge:{name:'熔爐天災・焚城機皇',kind:'core',color:'#ff6b38'},
frost:{name:'霜骨君王・白寂',kind:'storm',color:'#75dfff'},
marsh:{name:'毒孢神母・腐冠',kind:'brood',color:'#72ff88'},
temple:{name:'終末觀測者・虛空聖者',kind:'reaper',color:'#c77dff'}
};
const V6_ASCENSIONS={
ranger:[{name:'風暴獵王',desc:'射速 +35%、傷害 +25%、額外 1 發彈幕',apply:p=>{p.rate*=1.35;p.damage*=1.25;p.projectiles++}},{name:'穿界神射',desc:'爆擊 +25%、穿透 +2、傷害 +40%',apply:p=>{p.crit=Math.min(.9,p.crit+.25);p.pierce+=2;p.damage*=1.4}}],
knight:[{name:'不落聖壁',desc:'最大生命 +90、護甲 +18%、低血減傷強化',apply:p=>{p.maxHp+=90;p.hp+=90;p.armor=Math.min(.7,p.armor+.18);p.fortress=Math.max(p.fortress,.45)}},{name:'斷鋼戰王',desc:'傷害 +45%、射速 +20%、反傷 +100%',apply:p=>{p.damage*=1.45;p.rate*=1.2;p.thorns+=1}}],
mage:[{name:'星海賢者',desc:'連鎖 +30%、星爆冷卻縮短、傷害 +30%',apply:p=>{p.chain=Math.min(.95,p.chain+.3);p.nova=Math.max(2,p.nova?p.nova*.65:4);p.novaTimer=1;p.damage*=1.3}},{name:'虛空術皇',desc:'穿透 +2、額外 1 發、爆擊 +20%',apply:p=>{p.pierce+=2;p.projectiles++;p.crit=Math.min(.9,p.crit+.2)}}],
support:[{name:'神諭聖者',desc:'治療與護盾大幅強化，共鳴冷卻 -35%',apply:p=>{p.pulseHeal+=.055;p.pulseShield+=30;p.pulseCd=Math.max(1.7,p.pulseCd*.65)}},{name:'戰歌司祭',desc:'傷害 +35%、射速 +30%、浮游機 +2',apply:p=>{p.damage*=1.35;p.rate*=1.3;p.drones=Math.min(6,p.drones+2);p.pulseDamage*=1.6}}],
shadow:[{name:'永夜之皇',desc:'爆擊 +28%、速度 +20%、衝刺斬大幅強化',apply:p=>{p.crit=Math.min(.92,p.crit+.28);p.speed*=1.2;p.dashStrike+=2}},{name:'血月處刑者',desc:'處刑線提高到 35%、傷害 +50%、最大生命 +40',apply:p=>{p.execute=Math.max(p.execute,.35);p.damage*=1.5;p.maxHp+=40;p.hp+=40}}],
engineer:[{name:'機神統御者',desc:'砲塔上限 +2、砲塔傷害 +75%',apply:p=>{p.turretMax=Math.min(7,p.turretMax+2);p.turretDamage*=1.75}},{name:'裂隙爆破師',desc:'地雷冷卻 -45%、爆炸傷害 ×2、主武器 +30%',apply:p=>{p.mineCd=Math.max(1.4,p.mineCd*.55);p.mineDamage*=2;p.damage*=1.3}}],
elementalist:[{name:'三相大賢者',desc:'元素傷害 +40%、雷鏈 +30%、燃燒強化',apply:p=>{p.damage*=1.4;p.elementChain=Math.min(.95,p.elementChain+.3);p.burnPower*=1.8}},{name:'天災元素王',desc:'額外 2 發元素彈、射速 +25%、穿透 +1',apply:p=>{p.projectiles+=2;p.rate*=1.25;p.pierce++}}]
};
Object.assign(ENEMY_DEFS,{nullifier:{name:'禁制監察者',r:11,hp:72,speed:50,damage:16,color:'#a5a5ff',xp:15,score:350},prism:{name:'稜鏡蜂群',r:9,hp:54,speed:73,damage:14,color:'#ff86d9',xp:13,score:320},graviton:{name:'重力祭壇',r:14,hp:118,speed:28,damage:18,color:'#8d6cff',xp:18,score:410},mimic:{name:'裂隙擬態體',r:10,hp:66,speed:82,damage:20,color:'#ffc96a',xp:16,score:390}});
const _v6AvailableEnemyTypes=availableEnemyTypes;
availableEnemyTypes=function(){const a=_v6AvailableEnemyTypes();if(game.wave>=7)a.push('prism');if(game.wave>=9)a.push('nullifier');if(game.wave>=11)a.push('mimic');if(game.wave>=13)a.push('graviton');return a};
function v6RollRarity(source){const w=game?.wave||1,r=Math.random(),boss=source?.type==='boss';if(boss&&r<.16+w*.002)return V6_RARITIES[4];if(r<.03+w*.0015)return V6_RARITIES[4];if(r<.13+w*.002)return V6_RARITIES[3];if(r<.34)return V6_RARITIES[2];if(r<.66)return V6_RARITIES[1];return V6_RARITIES[0]}
function v6MakeItem(source){const rarity=v6RollRarity(source),slot=V6_SLOTS[Math.floor(Math.random()*V6_SLOTS.length)],m=rarity.mult,roll=(a,b)=>+(rand(a,b)*m).toFixed(3);const mods={};if(slot[0]==='weapon'){mods.damage=roll(.08,.18);mods.rate=roll(.04,.12);mods.crit=roll(.02,.06)}else if(slot[0]==='core'){mods.hp=Math.round(rand(12,32)*m);mods.armor=roll(.025,.07);mods.damage=roll(.03,.08)}else{mods.speed=roll(.04,.12);mods.crit=roll(.02,.07);mods.rate=roll(.03,.08)};const score=Math.round((mods.damage||0)*170+(mods.rate||0)*120+(mods.crit||0)*240+(mods.armor||0)*250+(mods.speed||0)*110+(mods.hp||0)*1.5);return{slot:slot[0],slotName:slot[1],rarity,name:`${rarity.name}・${slot[1]}`,mods,score}}
function v6ApplyGearMods(p,item,dir){if(!item)return;const m=item.mods;if(m.damage)p.damage*=dir>0?1+m.damage:1/(1+m.damage);if(m.rate)p.rate*=dir>0?1+m.rate:1/(1+m.rate);if(m.speed)p.speed*=dir>0?1+m.speed:1/(1+m.speed);if(m.crit)p.crit=clamp(p.crit+dir*m.crit,0,.95);if(m.armor)p.armor=clamp(p.armor+dir*m.armor,0,.75);if(m.hp){p.maxHp+=dir*m.hp;if(dir>0)p.hp=Math.min(p.maxHp,p.hp+m.hp);else p.hp=Math.min(p.hp,p.maxHp)}}
function v6Equip(p,item){const old=p.gear[item.slot];if(old&&old.score>=item.score){game.coins+=Math.max(1,Math.floor(item.score/18));toast(`${item.name} 分解為裂隙幣`);return}v6ApplyGearMods(p,old,-1);p.gear[item.slot]=item;v6ApplyGearMods(p,item,1);toast(`⬡ 裝備：${item.name}（戰力 ${item.score}）`);burst(p.x,p.y,item.rarity.color,16)}
function v6DropGear(e,owner){const elite=e.eliteAffixes?.length>0,chance=e.type==='boss'?1:elite?.38:.035;if(Math.random()>chance)return;const target=owner||alivePlayers()[Math.floor(Math.random()*Math.max(1,alivePlayers().length))];if(!target)return;const n=e.mutant?2:1;for(let i=0;i<n;i++)v6Equip(target,v6MakeItem(e))}
const _v6MakePlayer=makePlayer;makePlayer=function(hero,index,x,y){const p=_v6MakePlayer(hero,index,x,y);p.gear={weapon:null,core:null,charm:null};p.ascension=null;return p};
const _v6NewGame=newGame;newGame=function(){_v6NewGame();if(game){game.v6={ascended:false,eliteKills:0,loot:0};for(const p of game.players){p.gear=p.gear||{weapon:null,core:null,charm:null};p.ascension=null}}};
const _v6SpawnEnemy=spawnEnemy;spawnEnemy=function(initial=false,type=null){const before=game?.enemies.length||0;_v6SpawnEnemy(initial,type);if(!game||game.enemies.length<=before)return;const e=game.enemies[game.enemies.length-1];if(e.type==='boss'||e.type==='mini'||initial)return;const chance=Math.min(.08+game.wave*.006,.24);if(Math.random()<chance){const pool=[...V6_ELITES],count=game.wave>=18&&Math.random()<.35?2:1;e.eliteAffixes=[];for(let i=0;i<count;i++){const a=pool.splice(Math.floor(Math.random()*pool.length),1)[0];e.eliteAffixes.push(a.id);a.apply(e)}e.name='菁英·'+e.name;e.color='#ffe16d';e.maxHp*=1.28;e.hp=e.maxHp;e.damage*=1.12;e.xp*=2;e.score*=2.2;e.v6Tick=1.5}};
const _v6SpawnBoss=spawnBoss;spawnBoss=function(){_v6SpawnBoss();const b=game?.boss;if(!b)return;const mb=V6_MAP_BOSSES[game.map.id];if(mb){b.mapLord=true;b.bossKind=mb.kind;b.color=mb.color;b.bossName=(b.mutant?'變異·':'')+mb.name;b.maxHp*=1.35;b.hp=b.maxHp;b.damage*=1.15;b.v6Skill=2.4;toast(`♛ 地圖領主降臨：${b.bossName}`)}};
const _v6BossSkill=bossSkill;bossSkill=function(e){_v6BossSkill(e);if(!e.mapLord)return;const p=nearestPlayer(e);if(!p)return;const a=Math.atan2(p.y-e.y,p.x-e.x);if(game.map.id==='forge'){for(let i=0;i<12;i++)addEnemyBullet(e.x,e.y,i/12*TAU,205,e.damage*.32,5,'#ff733d')}else if(game.map.id==='frost'){for(let i=-3;i<=3;i++)addEnemyBullet(e.x,e.y,a+i*.12,190,e.damage*.38,5,'#8ee8ff','slow')}else if(game.map.id==='marsh'){e.hp=Math.min(e.maxHp,e.hp+e.maxHp*.025);for(let i=0;i<2;i++)game.enemies.push(makeEnemy('leech',{x:e.x+rand(-65,65),y:e.y+rand(-65,65)},1+game.wave*.05))}else if(game.map.id==='temple'){e.x=clamp(p.x+rand(-180,180),40,WORLD_W-40);e.y=clamp(p.y+rand(-180,180),40,WORLD_H-40);for(let i=0;i<10;i++)addEnemyBullet(e.x,e.y,i/10*TAU,180,e.damage*.34,4,'#d58cff')}else{damagePlayersInRadius(e.x,e.y,125,e.damage*.35,e)}};
const _v6UpdateEnemy=updateEnemy;updateEnemy=function(e,dt){_v6UpdateEnemy(e,dt);if(!game||e.dead)return;if(e.eliteAffixes?.includes('regen'))e.hp=Math.min(e.maxHp,e.hp+e.maxHp*.012*dt);if(e.type==='mimic'){e.v6Tick=(e.v6Tick||2.4)-dt;if(e.v6Tick<=0){e.v6Tick=2.8;const p=nearestPlayer(e);if(p){e.x=clamp(p.x+rand(-90,90),25,WORLD_W-25);e.y=clamp(p.y+rand(-90,90),25,WORLD_H-25)}}}if(e.type==='nullifier'||e.type==='prism'||e.type==='graviton'){e.v6Tick=(e.v6Tick||1.8)-dt;if(e.v6Tick<=0){e.v6Tick=e.type==='graviton'?2.8:2;const p=nearestPlayer(e);if(!p)return;const a=Math.atan2(p.y-e.y,p.x-e.x);if(e.type==='nullifier'){for(let i=-2;i<=2;i++)addEnemyBullet(e.x,e.y,a+i*.16,165,e.damage*.42,4,'#b8b8ff','slow')}else if(e.type==='prism'){for(let i=0;i<8;i++)addEnemyBullet(e.x,e.y,i/8*TAU,150,e.damage*.35,4,'#ff86d9')}else damagePlayersInRadius(e.x,e.y,150,e.damage*.32,e)}}};
const _v6Hurt=hurt;hurt=function(p,amount,ex,ey,source=null){const dealt=_v6Hurt(p,amount,ex,ey,source);if(dealt>0&&source?.eliteAffixes?.includes('vamp'))source.hp=Math.min(source.maxHp,source.hp+dealt*.6);return dealt};
const _v6KillEnemy=killEnemy;killEnemy=function(e,owner){const fresh=!e.dead,volatile=fresh&&e.eliteAffixes?.includes('volatile'),x=e.x,y=e.y,d=e.damage;_v6KillEnemy(e,owner);if(!fresh)return;if(e.eliteAffixes?.length)game.v6.eliteKills++;if(volatile){damagePlayersInRadius(x,y,105,d*.75,e);game.particles.push({type:'ring',x,y,r:8,maxR:110,life:.35,max:.35,color:'#ffcf68'})}v6DropGear(e,owner)};
function v6ShowAscension(){state='upgrade';game.v6.ascended=true;$('upgradeSubtitle').textContent='V6 進階轉職：選擇一條道路，全隊角色會依自身職業進化';const root=$('upgradeCards');root.innerHTML='';['A','B'].forEach((label,idx)=>{const el=document.createElement('article');el.className='upgrade';const lines=game.players.map(p=>{const a=V6_ASCENSIONS[p.hero]?.[idx];return a?`<b>${p.name} → ${a.name}</b><br><small>${a.desc}</small>`:''}).join('<hr>');el.innerHTML=`<h3>轉職道路 ${label}</h3><p>${lines}</p><span class="tag">V6 ASCENSION</span>`;el.onclick=()=>{for(const p of game.players){const a=V6_ASCENSIONS[p.hero]?.[idx];if(a){a.apply(p);p.ascension=a.name;p.name=a.name}}$('upgrade').classList.remove('show');state='playing';last=performance.now();toast('✦ 進階轉職完成！')};root.appendChild(el)});$('upgrade').classList.add('show')}
const _v6ShowUpgrade=showUpgrade;showUpgrade=function(){if(game?.level>=12&&!game.v6?.ascended){v6ShowAscension();return}_v6ShowUpgrade()};
function v6GearText(p){const slots=V6_SLOTS.map(([id,label])=>{const g=p.gear?.[id];return g?`<span style="color:${g.rarity.color}">${label}:${g.rarity.name}</span>`:`${label}:—`}).join('　');return `${p.ascension?`✦ ${p.ascension}　`:''}${slots}`}
const _v6UpdateHud=updateHud;updateHud=function(){_v6UpdateHud();let box=$('v6GearHud');if(!box){box=document.createElement('div');box.id='v6GearHud';box.style.cssText='position:absolute;left:10px;bottom:10px;max-width:75%;padding:7px 9px;background:rgba(9,6,29,.88);border:1px solid #6e5ab5;font:700 10px Courier New;line-height:1.6;z-index:7';$('hud').appendChild(box)}box.innerHTML=game.players.map(p=>`P${p.id} ${v6GearText(p)}`).join('<br>')};
const _v6SetupMode=setupMode;setupMode=function(){_v6SetupMode();$('modeSubtitle').textContent=playerCount===2?'V6 本機合作 × 裝備掉落 × 菁英詞綴 × 進階轉職 × 地圖領主':'V6 單人 Build × 裝備稀有度 × 菁英怪 × 進階轉職 × 地圖專屬 Boss';$('modeBadge').textContent=playerCount===2?'2 PLAYERS / V6 LOCAL CO-OP':'1 PLAYER / V6 ASCENSION'};