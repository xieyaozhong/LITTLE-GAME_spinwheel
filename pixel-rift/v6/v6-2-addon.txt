// PIXEL RIFT V6.2 addon: set gear, advanced elites, ascension ultimates, Apex lords
const V62_GEAR_AFFIXES=[
{id:'fury',name:'狂獵',apply:i=>{i.mods.damage=(i.mods.damage||0)+.10}},
{id:'haste',name:'超頻',apply:i=>{i.mods.rate=(i.mods.rate||0)+.08}},
{id:'vital',name:'巨像',apply:i=>{i.mods.hp=(i.mods.hp||0)+24}},
{id:'edge',name:'獵殺',apply:i=>{i.mods.crit=(i.mods.crit||0)+.055}},
{id:'ward',name:'堡壘',apply:i=>{i.mods.armor=(i.mods.armor||0)+.05}},
{id:'phase',name:'相位',apply:i=>{i.mods.speed=(i.mods.speed||0)+.08}}
];
const V62_SET_NAMES={wastes:'荒獵',forge:'熔心',frost:'霜誓',marsh:'腐生',temple:'虛界'};
const V62_SET_LABELS={wastes:'荒獵套裝',forge:'熔心套裝',frost:'霜誓套裝',marsh:'腐生套裝',temple:'虛界套裝'};
function v62GearScore(i){const m=i.mods||{};return Math.round((m.damage||0)*190+(m.rate||0)*135+(m.crit||0)*260+(m.armor||0)*280+(m.speed||0)*125+(m.hp||0)*1.65+(i.affixes?.length||0)*18+(i.setId?28:0))}
const _v62MakeItem=v6MakeItem;
v6MakeItem=function(source){const item=_v62MakeItem(source),rarityIndex=V6_RARITIES.findIndex(r=>r.id===item.rarity.id);if(source?.apex){const old=item.rarity.mult||1,target=V6_RARITIES[4];for(const k of Object.keys(item.mods)){item.mods[k]=k==='hp'?Math.round(item.mods[k]*target.mult/old):+(item.mods[k]*target.mult/old).toFixed(3)}item.rarity=target}const count=item.rarity.id==='legendary'?2:(item.rarity.id==='epic'||item.rarity.id==='rare'?1:0),pool=[...V62_GEAR_AFFIXES];item.affixes=[];for(let n=0;n<count&&pool.length;n++){const a=pool.splice(Math.floor(Math.random()*pool.length),1)[0];item.affixes.push(a.id);a.apply(item)}const setChance=item.rarity.id==='legendary'?.72:item.rarity.id==='epic'?.46:item.rarity.id==='rare'?.16:0;if(game?.map&&Math.random()<setChance){item.setId=game.map.id;item.setName=V62_SET_NAMES[item.setId]}item.score=v62GearScore(item);const affixText=item.affixes.length?'·'+item.affixes.map(id=>V62_GEAR_AFFIXES.find(a=>a.id===id)?.name||id).join('·'):'';item.name=`${item.rarity.name}・${item.setName?item.setName+'・':''}${item.slotName}${affixText}`;return item};
function v62ClearSetBonus(p){const b=p.v62SetBonus;if(!b)return;if(b.damage)p.damage/=b.damage;if(b.rate)p.rate/=b.rate;if(b.speed)p.speed/=b.speed;if(b.crit)p.crit=clamp(p.crit-b.crit,0,.95);if(b.armor)p.armor=clamp(p.armor-b.armor,0,.75);if(b.pierce)p.pierce=Math.max(0,p.pierce-b.pierce);if(b.hp){p.maxHp=Math.max(1,p.maxHp-b.hp);p.hp=Math.min(p.hp,p.maxHp)}p.v62SetBonus=null}
function v62RefreshSetBonus(p){v62ClearSetBonus(p);const counts={};for(const item of Object.values(p.gear||{}))if(item?.setId)counts[item.setId]=(counts[item.setId]||0)+1;let setId=null,count=0;for(const [id,n] of Object.entries(counts))if(n>count){setId=id;count=n}p.v62Set={id:setId,count};if(!setId||count<2)return;const b={damage:1,rate:1,speed:1,crit:0,armor:0,pierce:0,hp:0};if(setId==='wastes'){b.crit=.10;if(count>=3)b.damage=1.24}if(setId==='forge'){b.damage=1.16;if(count>=3)b.rate=1.18}if(setId==='frost'){b.armor=.09;if(count>=3)b.speed=1.2}if(setId==='marsh'){b.hp=42;if(count>=3)b.damage=1.12}if(setId==='temple'){b.rate=1.14;if(count>=3)b.pierce=1}p.damage*=b.damage;p.rate*=b.rate;p.speed*=b.speed;p.crit=clamp(p.crit+b.crit,0,.95);p.armor=clamp(p.armor+b.armor,0,.75);p.pierce+=b.pierce;if(b.hp){p.maxHp+=b.hp;p.hp=Math.min(p.maxHp,p.hp+b.hp)}p.v62SetBonus=b;toast(`◆ ${V62_SET_LABELS[setId]} ${count}/3 啟動`)}
const _v62Equip=v6Equip;
v6Equip=function(p,item){const before=p.gear?.[item.slot];_v62Equip(p,item);if(p.gear?.[item.slot]!==before)v62RefreshSetBonus(p)};
V6_ELITES.push({id:'phaseborn',name:'相位',apply:e=>{e.v62Phase=2.2}},{id:'aegis',name:'聖盾',apply:e=>{e.shield=(e.shield||0)+e.maxHp*.5;e.maxShield=e.shield}},{id:'plague',name:'瘟疫',apply:e=>{e.v62Plague=1.8}},{id:'conductor',name:'導電',apply:e=>{e.v62Arc=2.1}});
const V62_EXTRA_ELITES=V6_ELITES.slice(-4);
const _v62SpawnEnemy=spawnEnemy;
spawnEnemy=function(initial=false,type=null){const before=game?.enemies.length||0;_v62SpawnEnemy(initial,type);if(!game||game.enemies.length<=before)return;const e=game.enemies[game.enemies.length-1];if(initial||e.type==='boss'||e.type==='mini')return;if(e.eliteAffixes?.length){const wanted=game.wave>=36&&Math.random()<.45?3:game.wave>=24?2:e.eliteAffixes.length;while(e.eliteAffixes.length<wanted){const pool=V62_EXTRA_ELITES.filter(a=>!e.eliteAffixes.includes(a.id));if(!pool.length)break;const a=pool[Math.floor(Math.random()*pool.length)];e.eliteAffixes.push(a.id);a.apply(e)}if(wanted>=2&&!e.name.startsWith('異變菁英·'))e.name='異變菁英·'+e.name.replace(/^菁英·/,'')}};
const _v62UpdateEnemy=updateEnemy;
updateEnemy=function(e,dt){_v62UpdateEnemy(e,dt);if(!game||e.dead)return;if(e.eliteAffixes?.includes('phaseborn')){e.v62Phase=(e.v62Phase||2.2)-dt;if(e.v62Phase<=0){e.v62Phase=rand(2.1,3.4);const p=nearestPlayer(e);if(p){e.x=clamp(p.x+rand(-150,150),30,WORLD_W-30);e.y=clamp(p.y+rand(-150,150),30,WORLD_H-30);burst(e.x,e.y,'#bd8cff',8)}}}if(e.eliteAffixes?.includes('plague')){e.v62Plague=(e.v62Plague||1.8)-dt;if(e.v62Plague<=0){e.v62Plague=2.6;const p=nearestPlayer(e);if(p){const a=Math.atan2(p.y-e.y,p.x-e.x);for(let i=-1;i<=1;i++)addEnemyBullet(e.x,e.y,a+i*.18,135,e.damage*.28,5,'#78ff96','slow')}}}if(e.eliteAffixes?.includes('conductor')){e.v62Arc=(e.v62Arc||2.1)-dt;if(e.v62Arc<=0){e.v62Arc=2.8;for(let i=0;i<6;i++)addEnemyBullet(e.x,e.y,i/6*TAU,175,e.damage*.25,4,'#ffe477')}}};
const V62_ULT_NAMES={ranger:'天穹箭雨',knight:'王城聖壁',mage:'星海終焉',support:'神聖大合奏',shadow:'永夜處刑',engineer:'機神總動員',elementalist:'三相天災'};
function v62Ultimate(p){if(!p||p.dead||!p.ascension||p.v62UltT>0)return;const cd=p.hero==='support'?16:18;p.v62UltT=cd;const name=V62_ULT_NAMES[p.hero]||'裂隙奧義';toast(`✦ ${p.name}：${name}`);shake=10;if(p.hero==='ranger'){for(let i=0;i<24;i++){const a=i/24*TAU;game.bullets.push({owner:p,x:p.x,y:p.y,vx:Math.cos(a)*510,vy:Math.sin(a)*510,r:4,damage:p.damage*2.2,pierce:p.pierce+2,life:2,hit:new Set(),color:'#d9ffff',element:null})}}else if(p.hero==='knight'){p.shield=Math.min(p.maxHp,p.shield+p.maxHp*.75);p.invuln=Math.max(p.invuln,2);areaDamage(p.x,p.y,300,p.damage*3.2,p,'#ffe39a')}else if(p.hero==='mage'){areaDamage(p.x,p.y,430,p.damage*4.2,p,'#d3b8ff');for(const e of game.enemies)if(!e.dead&&d2(p,e)<520*520)damageEnemy(e,p.damage*1.4,p,false)}else if(p.hero==='support'){for(const q of alivePlayers()){q.hp=Math.min(q.maxHp,q.hp+q.maxHp*.5);q.shield=Math.min(q.maxHp*.8,q.shield+q.maxHp*.45)}areaDamage(p.x,p.y,360,p.damage*2.8,p,'#9dffd1')}else if(p.hero==='shadow'){const targets=game.enemies.filter(e=>!e.dead).sort((a,b)=>d2(p,a)-d2(p,b)).slice(0,10);for(const e of targets){game.particles.push({type:'line',x:p.x,y:p.y,x2:e.x,y2:e.y,life:.16,max:.16,color:'#8fb4ff'});damageEnemy(e,p.damage*4.5,p,false)}}else if(p.hero==='engineer'){for(let i=0;i<3;i++){const a=i/3*TAU;game.turrets.push({owner:p,x:clamp(p.x+Math.cos(a)*70,25,WORLD_W-25),y:clamp(p.y+Math.sin(a)*70,25,WORLD_H-25),life:18,shotT:.1,damage:p.damage*.85*p.turretDamage})}areaDamage(p.x,p.y,260,p.damage*2.5,p,'#ffb45f')}else if(p.hero==='elementalist'){areaDamage(p.x,p.y,380,p.damage*3.6,p,'#ff8cc8');for(let i=0;i<18;i++){const a=i/18*TAU;game.bullets.push({owner:p,x:p.x,y:p.y,vx:Math.cos(a)*420,vy:Math.sin(a)*420,r:4,damage:p.damage*1.8,pierce:p.pierce+1,life:2,hit:new Set(),color:i%3===0?'#ff765c':i%3===1?'#78dfff':'#ffe16d',element:i%3===0?'fire':i%3===1?'frost':'storm'})}}burst(p.x,p.y,p.color,28)}
addEventListener('keydown',e=>{if(e.key.toLowerCase()==='q')v62Ultimate(game?.players?.[0]);if(playerCount===2&&e.code==='ShiftRight')v62Ultimate(game?.players?.[1])});
const _v62SpawnBoss=spawnBoss;
spawnBoss=function(){_v62SpawnBoss();const b=game?.boss;if(!b)return;if(game.wave%30===0){b.apex=true;b.bossName='APEX·'+b.bossName;b.maxHp*=2.35;b.hp=b.maxHp;b.damage*=1.42;b.speed*=1.12;b.baseSpeed=b.speed;b.color='#fff06a';b.r+=7;b.v62ApexT=1.6;b.v62Phase=0;toast(`☄ APEX 領主覺醒：${b.bossName}`);shake=18}};
const _v62BossSkill=bossSkill;
bossSkill=function(e){_v62BossSkill(e);if(!e.apex)return;const p=nearestPlayer(e);if(!p)return;const a=Math.atan2(p.y-e.y,p.x-e.x);for(let i=0;i<20;i++)addEnemyBullet(e.x,e.y,i/20*TAU+game.t*.3,220,e.damage*.3,5,'#fff06a');for(let i=-2;i<=2;i++)addEnemyBullet(e.x,e.y,a+i*.1,300,e.damage*.42,5,'#ff8add')};
const _v62BossUpdate=updateEnemy;
updateEnemy=function(e,dt){_v62BossUpdate(e,dt);if(!game||e.dead||!e.apex)return;e.v62ApexT=(e.v62ApexT||1.5)-dt;const ratio=e.hp/e.maxHp;if(ratio<.66&&e.v62Phase<1){e.v62Phase=1;e.damage*=1.18;e.speed*=1.12;toast('☄ APEX 第二型態：超載')}if(ratio<.33&&e.v62Phase<2){e.v62Phase=2;e.damage*=1.2;e.speed*=1.12;e.shield=(e.shield||0)+e.maxHp*.12;toast('☄ APEX 最終型態：崩解')}if(e.v62ApexT<=0){e.v62ApexT=e.v62Phase>=2?1.4:2.1;for(let i=0;i<12;i++)addEnemyBullet(e.x,e.y,i/12*TAU+game.t,245,e.damage*.24,4,'#fff06a')}};
function v62InitPlayer(p){if(!p)return;p.v62UltT=p.v62UltT||0;if(p.v62GpUlt==null)p.v62GpUlt=false;p.v62Set=p.v62Set||{id:null,count:0}}
function v62EnsureHud(){if($('v62Hud'))return;const el=document.createElement('div');el.id='v62Hud';el.style.cssText='position:absolute;right:10px;bottom:10px;max-width:360px;padding:7px 9px;border:1px solid #6a5799;background:rgba(9,6,29,.88);font:700 10px Courier New;color:#ddd3ff;text-align:right;line-height:1.5;pointer-events:none';document.querySelector('.hud')?.appendChild(el)}
function v62HudText(){if(!game)return'';return game.players.map(p=>{v62InitPlayer(p);const set=p.v62Set?.id?`${V62_SET_NAMES[p.v62Set.id]} ${p.v62Set.count}/3`:'無套裝';const ult=p.ascension?(p.v62UltT>0?`奧義 ${p.v62UltT.toFixed(1)}s`:'奧義 READY'):'LV12 解鎖奧義';return `P${p.id} ${set}｜${ult}`}).join('<br>')+(game.boss?.apex?'<br><b style="color:#fff06a">☄ APEX LORD</b>':'')}
const _v62Update=update;
update=function(dt){if(game){for(const p of game.players){v62InitPlayer(p);p.v62UltT=Math.max(0,p.v62UltT-dt);if(p.v62Set?.id==='marsh'&&p.v62Set.count>=3&&!p.dead)p.hp=Math.min(p.maxHp,p.hp+p.maxHp*.006*dt);const pads=navigator.getGamepads?navigator.getGamepads():[],gp=pads[p.id-1],pressed=!!gp?.buttons?.[1]?.pressed;if(pressed&&!p.v62GpUlt)v62Ultimate(p);p.v62GpUlt=pressed}}const out=_v62Update(dt);v62EnsureHud();const hud=$('v62Hud');if(hud)hud.innerHTML=v62HudText();return out};
const _v62KillEnemy=killEnemy;
killEnemy=function(e,owner){const apex=!e.dead&&e.apex;_v62KillEnemy(e,owner);if(apex&&game){const target=owner||alivePlayers()[0];if(target){for(let i=0;i<2;i++)v6Equip(target,v6MakeItem({type:'boss',apex:true,mutant:true}));game.coins+=35;toast('☄ APEX 戰利品：保證傳說裝備 ×2')}}};
const _v62SetupMode=setupMode;
setupMode=function(){_v62SetupMode();$('modeBadge').textContent=playerCount===2?'2 PLAYERS / V6.2 APEX':'1 PLAYER / V6.2 APEX';$('modeSubtitle').textContent+=(playerCount===2?'｜P1 Q、P2 右 Shift 施放轉職奧義':'｜Q 施放轉職奧義')};
